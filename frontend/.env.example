# Frontend Environment Variables# Frontend Environment Variables

# Copy this file to .env for local development# API Base URL

# For production (Cloudflare Pages, Netlify, etc.), set these in your platform's environment settingsVITE_API_URL=http://localhost:8032/api

# API Base URL - where your backend API is hosted
# Examples:
# - For Cloudflare deployment with separate backend: https://api.bowl.soearthy.org/api
# - For Docker deployment with reverse proxy: https://bowl.soearthy.org/api
# - For local development: http://localhost:8032/api (or leave blank for auto-detection)
# - For same-domain deployment with reverse proxy: leave blank (will use /api)
VITE_API_BASE_URL=

# Example production configurations:
# VITE_API_BASE_URL=https://api.bowl.soearthy.org/api
# VITE_API_BASE_URL=https://bowl.soearthy.org/api

# Development default (if not set, app will auto-detect)
# VITE_API_BASE_URL=http://localhost:8032/api
